{
    "dependencies": {
        "axios": "^0.21.1",
        "cypress": "^7.2.0"
    },
    "private": true,
    "scripts": {
        "test": "DEBUG=cypress:* npx cypress run --headless --config video=false"
    }
}
