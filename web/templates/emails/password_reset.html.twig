{% trans_default_domain 'emails' %}

{% set password_reset_url = url('password_reset', {
    email: user.getEmail(),
    password_reset_code: user.getPasswordResetCode(),
}) %}

{{ 'greeting' | trans({
  '%user_username%': user.getUsername(),
}) }}

{{ 'password_reset.content' | trans }}
{{ password_reset_url | raw }}
